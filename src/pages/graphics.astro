---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { getGraphicsProjects } from "../data/projects.js";

const graphicsProjects = getGraphicsProjects();
---

<BaseLayout
  title="Graphics Projects - Esther Odeyemi"
  description="A collection of branding, packaging, advertising, and graphic design projects by Esther Odeyemi."
>
  <Header />

  <main class="project-detail">
    <section class="archive-hero site-shell section">
      <a href="/" class="project-back-link">← Home</a>

      <p class="project-label">Branding / Packaging / Advertising</p>
      <h1>Graphics Projects</h1>

      <p class="project-hero__summary">
        A collection of branding, packaging, advertising, and visual design
        projects focused on bold identity systems and campaign-ready visuals.
      </p>
    </section>

    <section class="work section archive-projects">
      <div class="site-shell">
        <div class="work__list">
          {graphicsProjects.map((project, index) => (
            <a
              href={project.link}
              class={`project-feature ${index % 2 === 1 ? "project-feature--flipped" : ""}`}
            >
              <figure class="project-feature__image">
                <img src={project.image} alt={`${project.title} project preview`} />
              </figure>

              <div class="project-feature__content">
                <div class="project-feature__meta">
                  <span class="project-feature__number">{project.number}</span>
                  <span>{project.category} / {project.year}</span>
                </div>

                <h3>{project.title}</h3>
                <p>{project.description}</p>

                <ul class="tag-list">
                  {project.tags.map((tag) => (
                    <li>{tag}</li>
                  ))}
                </ul>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>